# Custodio — backend

API FastAPI gestionada con [uv](https://docs.astral.sh/uv/). Ver el `README.md` en la raíz del
proyecto para instrucciones de instalación y uso completas.

```bash
uv sync
uv run uvicorn app.main:app --reload --port 8000
```
